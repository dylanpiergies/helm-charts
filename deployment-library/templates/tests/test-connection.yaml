apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deployment-library.fullname" . }}-test-connection"
  labels:
    {{- include "deployment-library.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deployment-library.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
